mixin service-item(title, img, link="#", color="#038696")
    a(href= link, style= `background-color: ${color}`).services-item

        .services-item__img
            img(src="./img/services/" + img, alt= title)

        .services-item__desc #{title}